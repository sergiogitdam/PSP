package edu.thepower.repasoprimerexamen;

public class ApuntesClasePreExamen {
    //Primer ejercicio: (factoría coches), condiciones de carrera + threads + estadísticas + join antes de sacar las estadísticas (metiendo los threads en arraylist)
    //Utilizar Atomic para identificadores únicos, evitar condición de carrera
    //Si nos piden crear por ejemplo una factoría de coches, utilizar una clase extra coche, cn constructor
    //Utilizar icrement and get para los atomic integer, es cómo i++
    //Crear aleatoriamente números enteros con Random, ehjemplo: Random r1 = new Random()
    //Para generar un nuevo random: r1.newInt(0, 10), esto sería un número entre 0 y 9
    //Para guardar los coches, usar listas, arrays. arraylists...
    //Guardar los usos de un semáforo/cualquier cosa en un Mapa(el primer valor del mapa el nombre del thread, un string, el segundo valor del mapa un AtomicInteger, contador)

    //Segundo ejercicio: deadlocks
}
